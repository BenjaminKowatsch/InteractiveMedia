version: '2'
services:
  mongo:
    image: mongo
    volumes:
      - /data:/data/db
  object-storage:
    build: ./minio
    volumes:
      - /minio-data:/data
    ports:
      - "9000:9000"
  backend:
    build: ./backend
    image: localhost:5000/backend
    ports:
      - "8081:8081"
    depends_on:
      - mongo
      - object-storage
  web:
    build: ./frontend
    image: localhost:5000/frontend
    ports:
      - 8088:80
    depends_on:
      - backend
